on:
  push:
    branches:
      - main
  pull_request:
jobs:
  build:
    runs-on: nixos
    outputs:
      outPath: ${{ steps.build.outputs.outPath }}
    steps:
      - uses: https://git.salame.cl/actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
      - run: nix --version
      - name: Build Package
        id: build
        run: |
          outPath=$(nix build --print-build-logs --print-out-paths .#)
          echo "outPath=packages:webnsupdate:$outPath" >> "$FORGEJO_OUTPUT"
  checks:
    runs-on: nixos
    needs: build # we use the built binaries in the checks
    strategy:
      matrix:
        system:
          - x86_64-linux
        test:
          - treefmt
          - clippy
          - deny
          - nextest
          - module-ipv4-only-test
          - module-ipv4-test
          - module-ipv6-only-test
          - module-ipv6-test
          - module-nginx-test
    env:
      TEST: ${{ matrix.test }}
      SYSTEM: ${{ matrix.system }}
    steps:
      - uses: https://git.salame.cl/actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
      - run: nix --version
      - name: Run tests
        run: |
          nix build --print-build-logs \
            .#checks."$SYSTEM"."$TEST"
  trigger-release:
    runs-on: nixos
    needs: checks
    steps:
      - uses: https://git.salame.cl/actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0 # fetch full history (needed for changelog)
          ref: ${{ forge.head_ref }} # checkout the branch instead of the pull request ref
          token: ${{ secrets.RELEASE_TOKEN }} # use the release token so we can trigger actions
      - run: nix --version
      - name: Create release PR
        env:
          GIT_TOKEN: "${{ secrets.RELEASE_TOKEN }}" # Token to create the release
          CARGO_REGISTRIES_GIT_SALAME_CL_TOKEN: "Bearer ${{ secrets.RELEASE_TOKEN }}" # Token to publish the package
          RUSTC_BOOTSTRAP: 1 # v0.3.6 uses nightly cargo features
        run: |
          nix develop .#release --command release-script
  report-size:
    runs-on: nixos
    needs: build
    steps:
      - uses: https://git.salame.cl/actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
      - run: nix --version
      - name: Generate size report
        uses: "https://git.salame.cl/jalil/nix-flake-outputs-size@41756d3f39618d7fc995b2a469d0859d6153e863" # main
        with:
          comment-on-pr: ${{ github.ref_name != 'main' }}
          generate-artifact: ${{ github.ref_name == 'main' }}
          do-comparison: true
          job-name: report-size
          prebuilt-derivations: "${{ needs.build.outputs.outPath }}"
